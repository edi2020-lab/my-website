<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /booking/

  # Serve actual files/dirs directly
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # Fallback → booking/index.html
  RewriteRule ^ /booking/index.html [L]
</IfModule>
